<div class="box">
  <h1>New show</h1>

  <% form_for(@show) do |f| %>
    <%= f.error_messages %>
    <%= f.text_field :name %>
    <p>
      <%= f.submit "Create" %>
    </p>
  <% end %>

  <%= link_to 'Back', shows_path %>
</div>